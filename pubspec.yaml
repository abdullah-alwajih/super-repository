name: super_repository
description: A new Flutter project.
publish_to: Abdullah-AlWajih
version: 0.2.14
homepage: https://github.com/Abdullah-AlWajih/super-repository

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # remote
  dio: ^5.0.2
  connectivity_plus: ^3.0.3

  # local
  get_storage: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1